# ForgeBoss v1.5.1 — Live Diagnosis Hotfix

Every cycle now builds and exposes its run report immediately instead of waiting for the whole autonomous session to end.
Safe refusals write their exact model summary into dashboard status and rebuild the live diagnosis immediately.
OPEN FULL DIAGNOSIS falls back to the live refusal reason if report generation itself is unavailable.
Activity now shows the actual refusal reason rather than only a generic 'gather evidence' message.
