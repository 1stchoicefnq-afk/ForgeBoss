# Stage 1 v28 R8 self-verifying candidate

Built from rejected v27 package UX/assets with the security/evidence stack rebased to the R8 engine line.

## Exact engine
- branch: `p0/stage1-package-verifier-r8`
- SHA: `356251956c8e9587877223f3dab877d586767e38`

## Candidate ZIP
- name: `ForgeBoss-Stage1-Turn-On-Pack-v28-R8-SELF-VERIFYING-CANDIDATE.zip`
- SHA-256: `c87856e3cc695e04fa133178c8abe686869b2e7abe131bd6e863285111be190d`

## Package gates
- static `PACKAGE-VERIFICATION.json` removed
- manifest SHA-256 + size + exact file-set enforced before install
- Python verifier behavior-scans every packaged `.py` for process-wide `os.mkdir` / `subprocess.Popen` monkeypatching, including renamed aliases and `setattr`
- 29/29 manifest-listed file tamper mutations independently rejected
- renamed `os.mkdir` monkeypatch independently rejected
- START/VERIFY engine identity derives from `stage1-installed.json`
- mismatched `FORGEBOSS_ENGINE_ROOT` rejected
- VERIFY is designed read-only; install creates a permanent read-only Docker probe
- Docker builder is digest-pinned to `python@sha256:227b6570d6ee07061ae6ca2eb04dedfb6d2b34045835f343065b9869e4d427ea`
- Windows runtime lock is generated by exact-head CI and installed with `pip --require-hashes --no-deps`
- authority no longer uses controller peer key as launch trust
- authority host continues with healthy named-pipe instances and only stops when all instances are quarantined
- package UI no longer process-wide monkeypatches stdlib process creation
- premium industrial dashboard HTML/assets retained
- raw Forge Vault secret storage disabled until scoped secret-broker proof exists
- hardcoded historical engine SHA removed from UI

## Exact-head CI evidence
R7 engine head passed Windows + Ubuntu P0 self-build bridge.
R8 package-security hostile regression jobs passed on Windows + Ubuntu.
R8 supply-chain image digest job passed.
R8 Windows fully hashed runtime-lock generation passed.

## Local independent candidate checks
- ZIP CRC: pass
- package verifier: pass
- Python package compile: pass before ZIP creation
- dashboard inline JavaScript `node --check`: pass
- 29/29 per-file append-byte mutations: detected
- renamed `os.mkdir` monkeypatch with matching updated manifest: detected

## Native owner acceptance still required
Do not treat the candidate as released or run paid P0 until the extracted package runs `VERIFY-FORGEBOSS.cmd` successfully on the owner's real Windows + Docker Desktop machine.
