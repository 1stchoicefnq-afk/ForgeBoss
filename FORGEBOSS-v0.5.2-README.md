# ForgeBoss v0.5.2 Control Center
All sidebar pages now work: Dashboard, Tasks, Workers, Costs, Activity and Settings. Merge and Deploy are owner toggles persisted locally, default OFF. They arm authority only; the current controller still does not auto-merge or deploy, and tests/review remain required. This prevents a UI toggle from silently becoming destructive execution.
