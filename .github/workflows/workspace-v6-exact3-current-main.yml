name: Workspace V6 exact3 current-main proof
on:
  push:
    branches:
      - forgeboss/worker-c-test-workspace-v6-exact3-current-main
permissions:
  contents: read
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify exact candidate tree Linux
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor 8c1d21dfba5ade416304849d4c479dcf2e19fcaf HEAD
          git diff --quiet 8c1d21dfba5ade416304849d4c479dcf2e19fcaf HEAD -- forgeboss
          test "$(git rev-parse 8c1d21dfba5ade416304849d4c479dcf2e19fcaf^{tree}:forgeboss)" = "$(git rev-parse HEAD^{tree}:forgeboss)"
      - name: Verify exact candidate tree Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          git merge-base --is-ancestor 8c1d21dfba5ade416304849d4c479dcf2e19fcaf HEAD
          if ($LASTEXITCODE -ne 0) { throw 'candidate not ancestor' }
          git diff --quiet 8c1d21dfba5ade416304849d4c479dcf2e19fcaf HEAD -- forgeboss
          if ($LASTEXITCODE -ne 0) { throw 'forgeboss tree changed' }
          $a=(git rev-parse '8c1d21dfba5ade416304849d4c479dcf2e19fcaf^{tree}:forgeboss').Trim()
          $b=(git rev-parse 'HEAD^{tree}:forgeboss').Trim()
          if ($a -ne $b) { throw 'forgeboss tree mismatch' }
      - name: Workspace V6 exact3 focused suite
        run: python -m unittest forgeboss.control.test_workspace -v
