name: Workspace V6 final2 current-main proof
on:
  push:
    branches:
      - forgeboss/worker-c-test-workspace-v6-final2-current-main
permissions:
  contents: read
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Verify exact candidate tree Linux
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          git merge-base --is-ancestor bd853cd6785a1c02df86a3297a155f80cf1b3871 HEAD
          git diff --quiet bd853cd6785a1c02df86a3297a155f80cf1b3871 HEAD -- forgeboss
          test "$(git rev-parse bd853cd6785a1c02df86a3297a155f80cf1b3871^{tree}:forgeboss)" = "$(git rev-parse HEAD^{tree}:forgeboss)"
      - name: Verify exact candidate tree Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          git merge-base --is-ancestor bd853cd6785a1c02df86a3297a155f80cf1b3871 HEAD
          if ($LASTEXITCODE -ne 0) { throw 'candidate not ancestor' }
          git diff --quiet bd853cd6785a1c02df86a3297a155f80cf1b3871 HEAD -- forgeboss
          if ($LASTEXITCODE -ne 0) { throw 'forgeboss tree changed' }
          $a=(git rev-parse 'bd853cd6785a1c02df86a3297a155f80cf1b3871^{tree}:forgeboss').Trim()
          $b=(git rev-parse 'HEAD^{tree}:forgeboss').Trim()
          if ($a -ne $b) { throw 'forgeboss tree mismatch' }
      - name: Workspace V6 final2 focused suite
        run: python -m unittest forgeboss.control.test_workspace -v
